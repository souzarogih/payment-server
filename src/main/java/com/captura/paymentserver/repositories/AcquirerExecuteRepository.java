package com.captura.paymentserver.repositories;

import com.captura.paymentserver.entitys.AcquirerExecute;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AcquirerExecuteRepository extends JpaRepository<AcquirerExecute, Long> {
}
