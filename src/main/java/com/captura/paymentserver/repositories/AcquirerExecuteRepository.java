package com.phoebus.paymentserver.repositories;

import com.phoebus.paymentserver.entitys.AcquirerExecute;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AcquirerExecuteRepository extends JpaRepository<AcquirerExecute, Long> {
}
